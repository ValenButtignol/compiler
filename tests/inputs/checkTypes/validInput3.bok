Program {
    bool foo(integer a) {
        return a == 1;
    }

    integer g(integer a) {
        return a + a; 
    }

    void main() {
        bool a = foo(g(g(1)));          // Error aca.
        return ;  
    }
}