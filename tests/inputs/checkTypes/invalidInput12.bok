Program {
    bool foo(integer a) {
        return true;
    }

    integer g(bool a) {
        return 1; 
    }

    void main() {
        bool a = foo(g(foo(g(foo(true)))));          // Error aca.
        return ;  
    }
}