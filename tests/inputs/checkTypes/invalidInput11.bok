Program {
    def bool foo(integer a) {
        return a == 1;
    }

    def integer g(integer a) {
        return a + a; 
    }

    def void main() {
        bool a = foo(g(g(1 < 2)));          // Error aca.
        return ;  
    }
}